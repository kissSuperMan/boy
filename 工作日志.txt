10-21
	移动每次一个像素点
10-25
	人物基础移动和地图跟随写完

10-27
	1.vector的问题非常讨厌. 不能随意的删除节点，
	2.MapUnit的单位定义的可以再小一点。（这个后来想象太难了，不做了。）
	3.物品奖励应放在哪？表中又如何表示
	4.

	所以决定，重写代码，顺便解决下其他的问题
	解决方案收集
		1.现在对 GameMap 的存储采用 List 双向存储，用自己的类型定义，确保
		2.对碰撞体的大小和渲染大小采用分开存储
			2.1 所以每个物体的 detory  清空屏幕 从渲染表数据删除。（依旧使用碰撞表，为了是将碰撞和渲染分开）
				对物体的每一次move做一次碰撞检查。
				火焰则在创建的时候检查碰撞。
	
		3.自定义表，表是一个双向的链表 template<typename T>		
			1.建立该表时，有一个初始化节点，存才成员变量里面（不是static的），方便找回开始位置。
		4.采用新的碰撞便利机制
		  得到这个点将在 Map 表中搜寻里面的 shape 如果有 shape 就会通过shape
				返回 来在碰撞表中查找这个shape点是属于谁的。
					这里要注意1.判断只判断移动的那个方向的最外层，因为后面的物体移动是一定能有shape的那是自己的shape
							  2.这样子人物作为非规则物体，在自己非主动移动时不会因为空白产生碰撞。	
		5.像素点保持不变，否则难度增加，

		6.在增加100的变量里面增加 奖励 和 入口 两个元素。
			Sence里面应该实现一个curSen的成员变量，当
		7.完善移动的像素点更新
10-28
没有做的事情：
	地图建立要大一点
	人物动画						（只添加了临时表情）
	关卡的后面两关 		
	开始和结束的画面
	picLoadInUnit 魔改出 对话Unit	（效果不好，有可能的）
11-3
	先完成第一个版本，消灭所有敌人视为游戏胜利
	否则游戏失败
11-3 
	导演类需要加载完全，初步实现 场景调度，完全实现结构体
	在明天上午能提交出一个完整的一个关卡能重新开始的版本
11-8
	讨论完后
	游戏性质发生改变
	是一个类似于魔塔的游戏，但是干掉怪物用的是炸弹，人物有血条
	奖励物品有4个
	1.炸弹数量+5的奖励
	2.全场固定1把的钥匙
	3.炸弹威力增加
	4.血量回复s

	关于掉血采用一个新的颜色来表示无敌的时间
	添加一个新的怪 图片用红色代替	移动速度更快

	按键： 按下马上相应，但是之后200ms不响应		ok


























